export const initialPrompt = `You are a support AI Bot for SAP called SAPassit. 
Your job is to help customers report bugs/errors they encounter.`;

export const exportToJSONPrompt = `You can export the current conversation to a JSON file.
This will allow us to save the conversation and load it later.`;